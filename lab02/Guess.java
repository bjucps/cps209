package app;

public class Guess {
    // Goals: 
    // * constructor that picks a random number [1, 10]
    // * constructor that sets the secret number to parameter value
    // * get/set methods for secret
    // * check method that provides String feedback on a guess
    // * set method for secret throws an IllegalArgumentException if the secret is < 1 or > 10
}
